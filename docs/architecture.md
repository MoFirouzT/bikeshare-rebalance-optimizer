# Architecture

What the pieces are, how they fit, and why the system is shaped this way.

> **A derived reading, not a source of truth.** Nothing here owns a claim.
> <!-- name the owners: the canonical document owns the model, conventions.md owns the
> units and names, the API doc owns the contract. --> This page exists so a reader meets
> the argument before the notation, and every load-bearing sentence links the source
> that settled it.

*Assumes: the [README](../README.md).*

---

## The argument

<!-- The design as one continuous case, in order: the commitment the project made, what
     follows from it, and each consequence with a link to the record that settled it.
     This is the document a new contributor reads start to finish. -->

## Capability map

<!-- One section per capability: what it does, which package implements it, what it
     depends on. Keep it stable; delivery detail belongs in the ledger. -->

## Layering

<!-- The dependency direction, and the check that enforces it. -->
